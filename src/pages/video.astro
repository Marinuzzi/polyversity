---
import BaseLayout from '../layouts/BaseLayout.astro';
import Section from '../components/Section.astro';
import { siteConfig } from '../config';
const basePath = import.meta.env.BASE_PATH || '';
---

<BaseLayout
  title="Intelligence — Briefing AI dal comando Ordine | POLYversity by Marinuzzi"
  description="Due briefing dal comando di intelligence artificiale per l'Ordine degli Ingegneri: 2 ore e 4 ore. Contenuto operativo, accesso diretto."
>
  <article>
    <Section class="pt-16 md:pt-24 pb-12">
      <h1 class="text-sm font-semibold uppercase tracking-widest text-black/60 mb-4">Intelligence</h1>
      <p class="text-2xl md:text-3xl font-semibold tracking-tight max-w-2xl">
        Due briefing dal comando AI per l'Ordine. 2 ore e 4 ore.
      </p>
    </Section>

    <Section narrow>
      <p class="text-lg leading-relaxed">
        Stesso intelligence fornito agli ufficiali (ingegneri). Registrazioni operative, senza censura. Per chi vuole studiare la strategia in autonomia.
      </p>

      <ul class="mt-10 space-y-8 list-none pl-0 border-t border-black/10 pt-10">
        <li>
          <span class="text-sm font-semibold uppercase tracking-wider text-black/60">Briefing 1</span>
          <p class="mt-2 text-lg">Circa 2 ore. Fondamenti e tattiche.</p>
        </li>
        <li>
          <span class="text-sm font-semibold uppercase tracking-wider text-black/60">Briefing 2</span>
          <p class="mt-2 text-lg">Circa 4 ore. Strategia avanzata e casi operativi.</p>
        </li>
      </ul>

      {siteConfig.videoPurchaseUrl ? (
        <p class="mt-12">
          <a
            href={siteConfig.videoPurchaseUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-block bg-black text-white px-6 py-3 font-medium hover:bg-black/90 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-black transition-colors"
          >
            Accedere ai file
          </a>
        </p>
      ) : (
        <p class="mt-12 text-black/60 text-sm">Link acquisto in configurazione (imposta PUBLIC_VIDEO_PURCHASE_URL).</p>
      )}
    </Section>
  </article>
</BaseLayout>

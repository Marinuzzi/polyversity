# Base path: vuoto per subdomain (polyversity.marinuzzi.it), /polyversity per path
BASE_PATH=

# Form: true = usa link esterno (Typeform/Google Form), false = form nativo
USE_EXTERNAL_FORM=false
EXTERNAL_FORM_URL=

# API apply (placeholder): URL endpoint per submit form nativo
APPLY_ENDPOINT=/api/apply
